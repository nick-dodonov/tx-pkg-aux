load("@tx-kit-ext//rules/build:tx_library.bzl", "tx_library")

tx_library(
    name = "pkg-http",
    srcs = glob(["**/*.cpp"]),
    hdrs = glob(["**/*.h"]),
    strip_include_prefix = ".",
    deps = [
        "//:pkg-log",
        "@asio//:asio",
        "@ada-url//:ada", #TODO: think to replace w/ cxxurl
    ],
    visibility = ["//visibility:public"],
)
