load("@tx-kit-ext//rules/build:tx_library.bzl", "tx_library")

tx_library(
    name = "pkg-log",
    srcs = glob(["**/*.cpp"]),
    hdrs = glob(["**/*.h"]),
    strip_include_prefix = ".",
    deps = [
        "@lwlog",
        "@spdlog//:spdlog", #? @spdlog//:header_only or @spdlog//:header_only_true
    ],
    visibility = ["//visibility:public"],
)
