load("@tx-kit-ext//rules:multi_app.bzl", "multi_app")

multi_app(
    name = "http",
    srcs = glob(["**/*.cpp"]),
    deps = [
        "//pkg/app",
        "//pkg/asio",
        "//pkg/http",
    ],
)
