load("@tx-kit-ext//rules:multi_app.bzl", "multi_app")

multi_app(
    name = "corosio",
    srcs = ["main.cpp"],
    deps = [
        "//src/pkg/boot",
        "@cppalliance.corosio",
    ],
    # platforms = ["host", "droid"]  # wasm doesn't have sockets
)
