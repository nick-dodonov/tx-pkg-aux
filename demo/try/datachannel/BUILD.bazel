load("@tx-kit-ext//rules:multi_app.bzl", "multi_app")

multi_app(
    name = "datachannel",
    srcs = ["copy-paste.cpp"],
    platforms = [
        "host",
        "droid",
    ],
    deps = [
        "@datachannel",
    ],
)

multi_app(
    name = "wsclient",
    srcs = ["wsclient.cpp"],
    cxxopts = [
        "-fexceptions",
    ],
    platforms = [
        "host",
        "droid",
    ],
    deps = [
        "@datachannel",
        "@nlohmann_json//:json",
    ],
)
