load("@tx-kit-ext//rules:multi_app.bzl", "multi_app")

multi_app(
    name = "hello_world",
    srcs = ["hello_world.cpp"],
    deps = [
        "@stdexec",
    ],
)

multi_app(
    name = "hello_coro",
    srcs = ["hello_coro.cpp"],
    deps = [
        "@stdexec",
    ],
)

multi_app(
    name = "hello_loop",
    srcs = ["hello_loop.cpp"],
    deps = [
        "//src/pkg/log",
        "@stdexec",
    ],
)
