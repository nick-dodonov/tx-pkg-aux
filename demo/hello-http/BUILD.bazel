load("@tx-kit-ext//rules/build:tx_binary.bzl", "tx_binary")

tx_binary(
    name = "hello-http",
    srcs = glob(["**/*.cpp"]),
    deps = [
        "//src/pkg-app",
        "//src/pkg-http",
    ]
)
