load("@tx-kit-ext//rules/build:tx_binary.bzl", "tx_binary")

tx_binary(
    name = "hello-http",
    srcs = ["main.cpp"],
    deps = ["//src/pkg-http"],
    cxxopts = ["-std=c++23"],
    #TODO: release full strip
    # strip -x hello-http
    # strip -s /dev/null hello-http
    # linkopts = [
    #     "-Wl,-unexported_symbols_list,/dev/null",
    #     "-Wl,-exported_symbols_list,$(location :exported_symbols.txt)",
    # ],
)
