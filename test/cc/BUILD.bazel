load("@tx-kit-ext//rules/build:multi_app.bzl", "multi_app")

multi_app(
    name = "bin",
    srcs = ["cc_src.cpp"],
    deps = [
        "//src/pkg-log",
        "@googletest//:gtest_main",
    ],
)
