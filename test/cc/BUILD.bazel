load("@tx-kit-ext//rules/build:multi_app.bzl", "multi_binary")

multi_binary(
    name = "bin",
    srcs = ["cc_src.cpp"],
    deps = [
        "//src/pkg-log",
        # "//src/pkg-boot",
        "@googletest//:gtest_main",
    ],
)
