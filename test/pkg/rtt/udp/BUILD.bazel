load("@tx-kit-ext//rules:multi_app.bzl", "multi_test")

multi_test(
    name = "udp",
    srcs = glob(["*.cpp"]),
    platforms = [
        "host",
        "droid",
    ],
    deps = [
        "//pkg/boot",
        "//pkg/rtt/udp",
        "//test/pkg/rtt/support",
        "@googletest//:gtest_main",
    ],
)
