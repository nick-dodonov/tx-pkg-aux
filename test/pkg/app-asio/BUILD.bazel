load("@tx-kit-ext//rules:multi_app.bzl", "multi_test")

multi_test(
    name = "app",
    srcs = ["app_asio_domain_test.cpp"],
    deps = [
        "//src/pkg-app-asio",
        "@googletest//:gtest_main",
    ],
)

multi_test(
    name = "fail",
    srcs = ["fail_app.cpp"],
    tags = ["manual"],  # Don't run this test automatically, only for manual checks
    deps = [
        "//src/pkg-app-asio",
        "@googletest//:gtest_main",
    ],
)
