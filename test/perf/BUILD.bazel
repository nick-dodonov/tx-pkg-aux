load("@tx-kit-ext//rules/build:multi_app.bzl", "multi_test")

multi_test(
    name = "call",
    srcs = ["call_test.cpp"],
    deps = [
        "//src/pkg-boot",
        "@google_benchmark//:benchmark",
    ],
)
