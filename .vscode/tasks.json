{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        // ================ dev environment
        {
            "type": "shell",
            "label": "compile_commands :refresh_all (HOST)",
            "command": "bazel run @hedron_compile_commands//:refresh_all",
            "group": {
                "kind": "build"
            }
        },
        {
            "type": "shell",
            "label": "compile_commands :refresh_all (WASM)",
            "command": "bazel run @hedron_compile_commands//:refresh_all -- --config=wasm",
            "group": {
                "kind": "build"
            }
        },
        {
            "type": "shell",
            "label": "compile_commands :refresh_all (DROID)",
            "command": "bazel run @hedron_compile_commands//:refresh_all -- --config=droid",
            "group": {
                "kind": "build"
            }
        },
        // ================ debug builds
        {
            "type": "shell",
            "label": "build <user>",
            "command": "bazel build ${config:tx.debugTaskName}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "focus": true
            }
        },
        {
            "type": "shell",
            "label": "sandbox-task",
            "command": "ls -l ${input:sandbox-input}.runfiles",
            "problemMatcher": []
        }
    ],
    "inputs": [
        {
            "id": "sandbox-input",
            "type": "command",
            "command": "shellCommand.execute",
            "args": {
                //"command": "echo \"$WORKSPACE\nsecond\"",
                "command": "bazel cquery --output=files ${config:tx.debugTaskName}",
                "cwd": "${workspaceFolder}",
                "useSingleResult": true
            }
        }
    ]
}