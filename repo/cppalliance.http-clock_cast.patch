--- src/server/serve_index.cpp	2025-01-01 00:00:00.000000000 +0000
+++ src/server/serve_index.cpp	2026-03-15 00:00:00.000000000 +0000
@@ -90,10 +90,9 @@
 std::uint64_t
 to_epoch(std::filesystem::file_time_type tp)
 {
-    auto const sctp = std::chrono::clock_cast<
-        std::chrono::system_clock>(tp);
-    auto const dur = sctp.time_since_epoch();
+    // Portable conversion without clock_cast (not available in all libc++ versions)
+    auto const dur = tp.time_since_epoch();
     return static_cast<std::uint64_t>(
         std::chrono::duration_cast<
             std::chrono::seconds>(dur).count());
 }
